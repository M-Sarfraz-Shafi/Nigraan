# Screenshots

`nigraan-demo.gif` — a ~10s clip showing the core loop: adding a task, extending it past its estimate (the overrun stripe appears), marking it done (color changes), and checking resource allocation. Already linked from the main [README.md](../../README.md).

Static screenshots (optional, not yet added) — drop these in if you want the Features section to have images too, the main README doesn't currently reference them so they're free-form:

| Filename | What to capture |
|---|---|
| `dashboard.png` | Dashboard view — the pie chart, Schedule Health card, and stat cards |
| `timeline.png` | Timeline view — a mix of statuses |
| `tasks.png` | Tasks list view — showing the Category / Est. / Actual Days / Comments columns |
| `members.png` | Members panel open on a project, showing at least one PO badge |
| `admin.png` | Admin screen, Users tab |
